# Identify Duplicate Images # 

## Purpose ## 
The purpose of this scrpit is to look at a folder or series of folders and detect then move those images to a new folder for review and removal by the user. 

## Requirements ## 
OpenCV: ```pip install opencv```

Hashlib: ```pip install hashlib```

Imageio: ```pip install imageio```

Matplotlib: ```pip install matplotlib```
